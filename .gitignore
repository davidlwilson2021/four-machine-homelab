# Temp extraction / build artifacts
_extract*/
_lab_arch*
_docx*
*.zip

# OS
.DS_Store
Thumbs.db

# Secrets (never commit)
*.env
.env.*
!.env.example
!**/.env.example
*.local.env
secrets/
*.pem
*.key
id_rsa*
credentials.json
docker-compose.override.yml
.smb-coldbackup
**/smb-credentials
!configs/mac-pro/smb-credentials.example

# TrueNAS / local overrides
configs/network/local-*.conf

# Security remediation artifacts (ops repo only)
.history_*
.commit_meta_*
.filter-repo-replacements.txt
.mailmap-redact
.literal_verify_post.txt
.tracked_post.txt
.replace-*.txt

# Handoff / session ops
files/

# Local agent memory (never public)
AGENTS.md
