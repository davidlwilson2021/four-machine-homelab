# Example SMB credentials file for cold-backup-rsync.sh (ops host only).
# Copy to e.g. /root/.smb-coldbackup, chmod 600, never commit.
# Set WD_CREDENTIALS_FILE to that path when scheduling the backup task.

username=backup_user
password=replace-with-long-random-password
